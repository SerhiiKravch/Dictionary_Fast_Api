# Frontend Placeholder

This directory is reserved for the future `React + Next.js` application.

Planned top-level structure:

```text
frontend/
  app/
  components/
  features/
  lib/
  public/
  services/
  types/
```

The backend has already been isolated into `backend/` so frontend tooling can be introduced here without colliding with Python files in the repository root.
